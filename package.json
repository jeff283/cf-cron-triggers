{
  "name": "cf-cron-triggers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "trigger-cron": "node scripts/trigger-cron.js"
  },
  "dependencies": {
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
